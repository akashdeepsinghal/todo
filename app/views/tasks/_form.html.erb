<%= form_for([@list, @list.tasks.build]) do |f| %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, placeholder: "New Todo" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
